using System.Collections.Generic;
using OsmSharp;

namespace Nixill.GTFS
{
  public class RouteShape
  {
    public RouteShape(Relation rel, Dictionary<long, BusStop> stops)
    {
      // Turns a relation into a RouteShape

    }
  }
}